#!/usr/bin/env bash
# DigiHub UI helpers (colors + layout)
# Safe to source from any bash script.

# ---- Color helpers (auto-off if not a terminal / NO_COLOR set) ----
color_on=true
if [ ! -t 1 ] || [ -n "${NO_COLOR:-}" ]; then
  color_on=false
fi

c() { $color_on && tput "$@" || true; }

RESET="$(c sgr0)"
BOLD="$(c bold)"
DIM="$(c dim)"

FG_WHITE="$(c setaf 7)"
FG_CYAN="$(c setaf 6)"
FG_GREEN="$(c setaf 2)"
FG_YELLOW="$(c setaf 3)"
FG_RED="$(c setaf 1)"
FG_BLUE="$(c setaf 4)"

# Normal output styles
HEAD="${BOLD}${FG_CYAN}"
LABEL="${BOLD}${FG_WHITE}"
VALUE="${FG_WHITE}"
MUTED="${DIM}${FG_WHITE}"

# Error output styles
ERR_HEAD="${BOLD}${FG_RED}"
ERR_LABEL="${BOLD}${FG_WHITE}"
ERR_VALUE="${FG_WHITE}"

section() { # section "Title"
  printf "\n${BOLD}${FG_BLUE}%s${RESET}\n" "$1"
}

kv() { # kv "Label" "Value"
  printf "  ${LABEL}%-14s${RESET} ${VALUE}%s${RESET}\n" "$1" "${2:-N/A}"
}

err_section() { # err_section "Title"
  printf "\n${ERR_HEAD}%s${RESET}\n" "$1" >&2
}

err_kv() { # err_kv "Label" "Value"
  printf "  ${ERR_LABEL}%-14s${RESET} ${ERR_VALUE}%s${RESET}\n" "$1" "${2:-}" >&2
}

die() { # die "Title" "Message" [exit_code]
  local title="${1:-Error}"
  local msg="${2:-}"
  local code="${3:-1}"
  err_section "$title"
  [ -n "$msg" ] && err_kv "Message" "$msg"
  printf "\n" >&2
  exit "$code"
}

need_file() { # need_file "/path/to/file" "what to do"
  local f="$1"
  local action="${2:-}"
  if [ ! -r "$f" ]; then
    err_section "Configuration Missing"
    err_kv "File" "$f"
    [ -n "$action" ] && err_kv "Action" "$action"
    printf "\n" >&2
    exit 1
  fi
}

run_sudo() { # run_sudo <command> [args...] -- skips sudo if already root
  if [ "${EUID}" -eq 0 ]; then
    "$@"
  else
    sudo "$@"
  fi
}

# systemctl only knows about units already present in /etc/systemd/system;
# on-demand services (not enabled unconditionally by install.sh) only have
# their template cached in /usr/local/lib/digihub until something actually
# needs them. Installs one on first use, rendering __DIGIHUB_USER__ the
# same way install.sh does. Returns non-zero (with a message on stderr) if
# the cached template is missing or the install fails.
ensure_unit_installed() { # ensure_unit_installed "some.service"
  local dest="/etc/systemd/system/$1" src="/usr/local/lib/digihub/$1" tmp

  run_sudo test -f "$dest" && return 0

  if [ ! -f "$src" ]; then
    printf 'Error: unit template not found: %s\n' "$src" >&2
    printf 'Run dhupdate to restore DigiHub'"'"'s installed files.\n' >&2
    return 1
  fi

  tmp="$(mktemp)"
  sed -e "s|__DIGIHUB_USER__|$(id -un)|" "$src" > "$tmp"
  if ! run_sudo install -m 0644 "$tmp" "$dest"; then
    rm -f "$tmp"
    printf 'Error: could not install unit file: %s\n' "$dest" >&2
    return 1
  fi
  rm -f "$tmp"
  run_sudo systemctl daemon-reload || true
}

status_tag() {
  case "$1" in
    Active)
      printf "%sACTIVE%s" "$FG_GREEN" "$RESET"
      ;;
    Expired)
      printf "%sEXPIRED%s" "$FG_RED" "$RESET"
      ;;
    *)
      printf "%sUNKNOWN%s" "$FG_YELLOW" "$RESET"
      ;;
  esac
}

# Starts a TigerVNC desktop (:1) plus a noVNC websocket bridge (port
# 6080), then execs into a GUI app as the final foreground process --
# shared by DigiHub's VNC-backed dhmode entries (wsjtx/js8call/qsstv),
# which need exclusive access to the same audio device Direwolf/ardopcf
# use, so unlike aprsd (Phase 7) they're real dhmode modes, not
# independent toggles. Never returns on success (execs into $1).
# Usage: run_vnc_session <app-binary> <wmctrl-title-substring>
run_vnc_session() {
  local app="$1" title="$2"

  # Defensive cleanup of a stale session from a prior crashed run --
  # systemd's KillMode=control-group handles a clean stop already, this
  # only matters if something died without systemd noticing.
  vncserver -kill :1 >/dev/null 2>&1 || true
  pkill -f novnc_proxy >/dev/null 2>&1 || true
  sleep 1

  # vncserver (tigervncserver) daemonizes itself; novnc_proxy is
  # backgrounded explicitly. Both live in this unit's cgroup, so
  # systemd cleans them up on stop even though only $app itself is the
  # tracked foreground process.
  vncserver :1 -depth 16 -localhost yes
  /usr/share/novnc/utils/novnc_proxy --vnc localhost:5901 --listen 6080 >/dev/null 2>&1 &

  export DISPLAY=:1

  # Best-effort auto-maximize; harmless if it never matches.
  (
    for _ in $(seq 1 20); do
      wmctrl -a "$title" -b add,maximized_vert,maximized_horz >/dev/null 2>&1 && break
      sleep 1
    done
  ) &

  exec "$app"
}
